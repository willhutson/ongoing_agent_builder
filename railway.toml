[build]
builder = "DOCKERFILE"
dockerfilePath = "modules/Dockerfile.combined"
watchPatterns = ["modules/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
